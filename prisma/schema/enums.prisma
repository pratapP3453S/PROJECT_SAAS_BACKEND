// ─── Shared Enums ─────────────────────────────────────────────────────────────
// All enums are defined here so they can be referenced across schema files.

enum Role {
  SUPER_ADMIN
  ADMIN
  USER
}

enum UserStatus {
  ACTIVE
  INACTIVE
  SUSPENDED
  PENDING_VERIFICATION
}

enum UploadStatus {
  PENDING
  PROCESSED
  FAILED
}
