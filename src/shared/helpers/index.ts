/**
 * shared/helpers — placeholder barrel for feature-agnostic helper functions.
 *
 * Use this folder for stateless helpers that don't belong in `utils/` (which
 * holds general-purpose primitives like date math and crypto) and don't warrant
 * a service. Examples that would land here:
 *   - request-scoped formatting helpers (currency, locale)
 *   - typed wrapper functions around third-party libraries
 *   - small domain-neutral mappers
 *
 * Add files here and re-export them from this barrel.
 */
export {};
